"""Reads what the page fetched for itself, out of bodies. Knows no site: finds lists of objects
with shared keys in a structured body, falls back to CSV, and derives what it can see so blind
refuses rather than guesses.
"""

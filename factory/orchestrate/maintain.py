"""The caller that makes amortize and demote real. Nothing else re-examines a capability that
stopped paying or an entry a refutation should drop. A gate with no call site is the defect this
exists to avoid.
"""
